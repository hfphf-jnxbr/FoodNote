package com.example.foodnote.ui.calorie_calculator_fragment.const

import com.example.foodnote.R

enum class FragmentIndex(val value: Pair<Int, Int>) {
    CIRCLE_FRAGMENT(Pair(0, R.string.calorie)),
    WATER_FRAGMENT(Pair(1, R.string.water))
}